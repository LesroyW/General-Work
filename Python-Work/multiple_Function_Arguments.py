#Python receives a predefined number of arguments, if declared normally like this:
#Also possible to declare functions which recieve a variable number of arguments using *collection
#This variable is a list of varibales which receives the rest of the arguments after the first
# Additionally it is possible to send function arguments by keyword, order of the argument doesn't matter
# this is done using **options parameter
